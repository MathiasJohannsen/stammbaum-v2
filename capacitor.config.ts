import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'stammbaum-v2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
